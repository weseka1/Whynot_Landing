/* ============================================================================
   FOOTER  —  vaciado por pedido del usuario.

   El footer original mostraba: el nombre de marca gigante (DICH/WHYNOT),
   4 slots de creditos (SITE BY / MASTERCLASS BY / AWWWARDS / LEGAL) con
   placeholders TODO, lista de external links, y copyright. El usuario
   pidio eliminar todo eso directamente, asi que el componente queda
   como no-op (no renderiza nada). Se mantiene el archivo para no romper
   el import en app/page.tsx.
   ============================================================================ */

export default function Footer() {
  return null;
}
