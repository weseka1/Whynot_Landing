# sneaker-ultra-pro — Python 3.11 recomendado
# CUDA opcional. Para NVIDIA reinstalar torch con:
#   pip install --upgrade --force-reinstall torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ---------- Core ----------
numpy>=1.26,<3
Pillow>=10.3
opencv-contrib-python>=4.10        # incluye ximgproc (guided filter, joint bilateral)
tqdm>=4.66
scipy>=1.13
scikit-image>=0.24

# ---------- Deep learning ----------
torch>=2.4
torchvision>=0.19
transformers>=4.45                  # VITMatte (hustvl/vitmatte-base)
accelerate>=0.30                    # auto device-map para HF

# ---------- Segmentacion primaria + refinement ----------
rembg>=2.0.65                       # wrapper BiRefNet/ISNet/U2Net ONNX
onnxruntime>=1.18                   # CPU. Para GPU NVIDIA: onnxruntime-gpu
huggingface_hub>=0.24

# ---------- SAM2 (instalar con install.bat -WithSam2) ----------
# git+https://github.com/facebookresearch/sam2.git
hydra-core>=1.3
iopath>=0.1.10

# ---------- Alpha matting clasico (fallback) ----------
pymatting>=1.1.12

# ---------- Utilidades ----------
einops>=0.8
omegaconf>=2.3
